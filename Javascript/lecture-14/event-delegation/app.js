// let allfig= document.querySelectorAll('#section figure')

// let section= document.querySelectorAll('#section')
// for(let item of allfig){
//     item.addEventListener('click',function(e){
//         console.log(e.target.innerText)
//     })
// }
// let figure = document.querySelector("#section")
// section.innerText=10;
// section.appendChild(figure)

// section.addEventListener('click',function(e){
//     if(e.target.nodeName==='FIGURE'){
//         console.log(e.target.innerText)
//     }
// })

// the process of attaching event to a single parent rather than disturbiting events to the multiple to the event this solves the problem of
// dynamically created elementover which we want the event to be triggered  
